function fn() {
  var config = {
    hosts: {
      supergraph: 'http://localhost:4000',
      account: 'http://localhost:4001',
      customer: 'http://localhost:4002',
    },
  };

  return config;
}
